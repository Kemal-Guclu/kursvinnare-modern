/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["assets.coingecko.com", "cdn.mydomain.com"], // Lägg till fler om du använder andra API:er
  },
};

export default nextConfig;
